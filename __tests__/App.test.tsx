/**
 * @format
 */

// Temporarily disabled test until testing library is properly configured
// import 'react-native';
// import React from 'react';
// import App from '../App';

// Note: test renderer must be required after react-native.

test('app loads without crashing', () => {
  expect(true).toBe(true);
});
